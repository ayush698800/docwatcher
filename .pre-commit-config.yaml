repos:
  - repo: local
    hooks:
      - id: docdrift
        name: DocDrift - check for stale docs
        entry: python -m docwatcher.cli precommit
        language: system
        pass_filenames: false
