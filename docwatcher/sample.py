# This file is for testing DocDrift locally
# Add your own functions here to see DocDrift in action

def example_function(name: str, age: int) -> str:
    """Returns a greeting string."""
    return f"Hello {name}, you are {age} years old"

class ExampleClass:
    def example_method(self, value: int) -> bool:
        """Returns True if value is positive."""
        return value > 0